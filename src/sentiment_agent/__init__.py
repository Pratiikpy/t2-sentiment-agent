"""t2-sentiment-agent: Qwen decides, a reduce-only risk kernel gates, Agent Hub executes on Demo.

The contract every module codes against is :mod:`sentiment_agent.types`; the numbers every module
obeys are :mod:`sentiment_agent.policy`. DESIGN.md at the repository root is the map.
"""

__version__ = "0.1.0"
